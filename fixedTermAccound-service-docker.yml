server:
  port: 8084

spring:
  data:
    mongodb:
      uri: mongodb://mongo-server:27017/ProyectoS1Bank
      auto-index-creation: true
  jpa:
    hibernate:
      ddl-auto: update
  application:
    name: fixedTermAccound-service

eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka-server:8761/eureka/