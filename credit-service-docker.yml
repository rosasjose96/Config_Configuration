server:
  port: 8090

spring:
  data:
    mongodb:
      uri: mongodb://mongo-server:27017/BootcampEverisS1
  application:
    name: credit-service

eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka-server:8761/eureka/
